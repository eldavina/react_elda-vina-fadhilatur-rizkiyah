import { render, screen } from "@testing-library/react";
import renderer from "react-test-renderer";
import { describe, expect, it, vi } from "vitest";
import CreateProduct from "./CreateProduct.view";

// import { assertType, describe, expectTypeOf, it } from "vitest";
// import CreateProduct from "./CreateProduct.view";

describe("CreateProduct.view.jsx", () => {
	// 	it("should render correctly", () => {
	// 		const tree = renderer.create(<CreateProduct />).toJSON();
	// 		expect(tree).toMatchSnapshot();
	// 	});

	it("should render correct value if input type of name is false", () => {
		render(<CreateProduct name={true} onSubmit={vi.fn()} />);
	});

	expect(screen.getByText("Bambang")).toBeInDocument();
});

// describe("Create Product Name", () => {
//     it('name', () => {

//     })
// })

// test('name product type', () => {
//     expectTypeOf(CreateProduct).toBeFunction()
//     expectTypeOf(CreateProduct).parameter(0).toMatchTypeOf<{name : string}>()

//     assertType(CreateProduct({name:Bambang}))
// });
